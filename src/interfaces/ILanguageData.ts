/**
 * Representa os dados de um idioma.
 */
export interface ILanguageData {
    /** Código ISO 639‑1 (alpha‑2). */
    alpha2: string;
    /** Código ISO 639‑2 (alpha‑3). */
    alpha3: string;
    /** Nome do idioma. */
    name: string;
  }
  